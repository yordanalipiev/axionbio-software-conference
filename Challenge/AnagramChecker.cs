namespace Challenge
{
    public class AnagramChecker
    {
        /// <summary>
        /// Implement a method that checks if two strings are anagrams of each other.
        /// Two strings are anagrams if they contain the same characters with the same frequencies, ignoring spaces and case.
        /// For example, "Listen" and "Silent" are anagrams.
        /// </summary>
        /// <param name="first">The first string to check.</param>
        /// <param name="second">The second string to check.</param>
        /// <returns>True if the two strings are anagrams, false otherwise.</returns>
        public bool AreAnagrams(string first, string second)
        {
            throw new NotImplementedException();
        }
    }
}
