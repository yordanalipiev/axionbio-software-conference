namespace Challenge
{
    public class FibonacciCalculator
    {
        /// <summary>
        /// Given an integer n, implement a method that returns the nth Fibonacci number.
        /// The Fibonacci sequence is defined as:
        /// Fib(0) = 0, Fib(1) = 1, and Fib(n) = Fib(n-1) + Fib(n-2) for n >= 2.
        /// </summary>
        /// <param name="n">The position in the Fibonacci sequence to retrieve.</param>
        /// <returns>The nth Fibonacci number.</returns>
        /// <exception cref="ArgumentException"> If negative number is provided. </exception>
        public int GetFibonacci(int n)
        {
            throw new NotImplementedException();
        }
    }
}
